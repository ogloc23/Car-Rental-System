export const sendNotification = async (userId: string, message: string) => {
    console.log(`📢 Notification for User (${userId}): ${message}`);
    // In a real app, integrate with an email/SMS service here.
  };
  